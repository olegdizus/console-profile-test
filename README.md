# How console statements slow down code execution
Profiling function with and without console.info

## Usage
1. Open `index.html` in browser and open browser console to check result
2. Run `node main.js` in terminal
